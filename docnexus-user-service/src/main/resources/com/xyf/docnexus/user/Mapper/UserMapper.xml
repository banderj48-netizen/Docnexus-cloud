<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyf.docnexus.user.Mapper.UserMapper">
    <select id="selectByUsername" resultType="com.xyf.docnexus.common.DTO.UserDTO">
        select id, username, password, email, phone, role, status, token_version as tokenVersion, create_time as createTime
        from user_account
        where username = #{username}
        limit 1
    </select>

    <select id="countByUsername" resultType="int">
        select count(1)
        from user_account
        where username = #{username}
    </select>
    <select id="selectTokenVersionById" resultType="java.lang.Long">
        SELECT token_version
        FROM user_account
        WHERE id = #{id}
        LIMIT 1
    </select>
    <select id="selectById" resultType="com.xyf.docnexus.common.DTO.UserDTO">
        select id, username, password, email, phone, role, status, token_version as tokenVersion, create_time as createTime
        from user_account
        where id = #{id}
        limit 1
    </select>


    <insert id="insertUser" parameterType="com.xyf.docnexus.common.pojo.User">
        insert into user_account (
            username,
            password,
            email,
            phone,
            role,
            status,
            create_time,
            update_time
        ) values (
                     #{username},
                     #{password},
                     #{email},
                     #{phone},
                     #{role},
                     #{status},
                     #{createTime},
                     #{updateTime}
                 )
    </insert>

    <update id="updatePasswordById">
        update user_account
        set password = #{password},
            update_time = now()
        where id = #{id}
    </update>
    <update id="increaseTokenVersionById">
        UPDATE user_account
        SET token_version = token_version + 1,
            update_time = NOW()
        WHERE id = #{id}
    </update>
    <update id="updateProfileById" parameterType="com.xyf.docnexus.common.DTO.UserProfileUpdateRequest">
        update user_account
        <set>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            update_time = now()
        </set>
        where id = #{userId}
    </update>
</mapper>
