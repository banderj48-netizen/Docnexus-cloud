# 阿里云服务器 frps 配置补充示例。
# 如果服务器上已经有 frps.ini，不要整文件覆盖，只需要确认 bind_port、token 与现有配置一致，并把新端口加入 allow_ports。

[common]
bind_port = 7000
token = 请替换为已有_frp_token

# 允许 frpc 新增前端远程端口。这里预留 18173 给 DocNexus 前端开发服务。
# 如果你的旧配置已经有 allow_ports，需要把 18173 合并进去，例如：8090,18173 或 18000-18999。
allow_ports = 18173
